import { IProduct } from "../../types/ProductsType";


const UpdateModal = ({ data }: IProduct) => {
    console.log(data)
    return (
        <div>
            it is update modal
        </div>
    );
};

export default UpdateModal;